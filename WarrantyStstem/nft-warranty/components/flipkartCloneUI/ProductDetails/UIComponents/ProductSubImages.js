import classes from './ProductSubImages.module.css'; 

const ProductSubImages = (props) => {
    return(
        <div className={classes.back}>
            <p>dnsjk</p>
        </div>
    );
};

export default ProductSubImages;